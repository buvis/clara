SECRET_KEY=change-me-to-random-string
# Separate key for Fernet encryption (TOTP secrets, integration credentials).
# If unset, falls back to SECRET_KEY. Set this so you can rotate SECRET_KEY
# without breaking encrypted data.
ENCRYPTION_KEY=
DATABASE_URL=postgresql://clara:clara@localhost:5432/clara
REDIS_URL=redis://localhost:6379/0
DEBUG=true
CORS_ORIGINS=["http://localhost:5173"]
COOKIE_SECURE=false
COOKIE_DOMAIN=localhost
