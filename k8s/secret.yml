apiVersion: v1
kind: Secret
metadata:
  name: clara-secret
  namespace: clara
type: Opaque
stringData:
  # Replace these placeholder values before applying.
  # Use: kubectl create secret generic clara-secret --from-literal=... --dry-run=client -o yaml
  SECRET_KEY: "CHANGE-ME-generate-with-openssl-rand-hex-32"
  DATABASE_URL: "postgresql://clara:CHANGE-ME@postgres:5432/clara"
